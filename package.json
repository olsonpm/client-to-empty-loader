{
  "name": "client-to-empty-loader",
  "version": "0.1.0",
  "description": "Replaces require statements containing '/client/' with an empty object",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "phil olson",
  "license": "ISC"
}
